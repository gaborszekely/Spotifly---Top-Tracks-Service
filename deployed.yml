config:
  target: 'http://sdc-tcp-load-balancer-bcd1fa8871a2a068.elb.us-east-2.amazonaws.com/tracks'
  # plugins:
  # expect: {}
  phases:
    - duration: 60
      arrivalRate: 1000
scenarios:
  - name: Get top tracks
    flow:
      - get:
          url: '/top'
          # capture:
          #   - json: 'name'
          # expect:
          #   - statusCode: 200
          #   - contentType: json
          # - hasProperty: "name"
          # - equals:
          #     - "Standing Yellow Flannel"
